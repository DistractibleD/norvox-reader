"""Single source of truth for the app's version number. Update this on
each release; everything else (Settings display, build scripts, the
future auto-update check) should read from here rather than duplicating
the string."""

__version__ = "0.2.0"
